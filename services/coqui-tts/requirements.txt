# System dependency (install before pip deps):
#   Arch:         sudo pacman -S espeak-ng
#   Ubuntu/Debian: sudo apt install espeak-ng

torch
torchaudio
coqui-tts[codec]
fastapi
uvicorn[standard]
soundfile
